{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prisma:deploy": "npx prisma generate && npx prisma migrate deploy",
    "dev:docker:migrate": "npm run prisma:deploy && node prisma/seed.js && node index.js",
    "dev:docker": "npm run prisma:deploy && node index.js",
    "prod:seed": "npm run prisma:deploy && node prisma/seed.js && node index.js",
    "prod": "npm run prisma:deploy && node index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^5.16.1",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1",
    "nodemon": "^3.1.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "prisma": "^5.16.1"
  }
}
